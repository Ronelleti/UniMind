{{- if .Values.frontend.enabled }}

apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: {{ .Release.Namespace }}

spec:
  selector:
    app: frontend

  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 80

  type: ClusterIP

{{- end }}
